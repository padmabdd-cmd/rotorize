<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("partials/head", { meta: meta, site: site }) %>

  <!-- Styles -->
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@700&family=Poppins:wght@400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    html, body {
      margin: 0;
      padding: 0;
      background-color: #ff9a00;
      color: #fff;
      font-family: 'Poppins', sans-serif;
      overflow-x: hidden;
    }

    /* ---------- HERO SECTION ---------- */
    .page-hero {
      position: relative;
      height: 80vh;
      background: url('/images/drone.jpeg') center center / cover no-repeat fixed;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      overflow: hidden;
    }

    .page-hero::after {
      content: "";
      position: absolute;
      inset: 0;
      background: rgba(0,0,0,0.45);
      z-index: 1;
    }

    .page-hero h1,
    .page-hero p {
      position: relative;
      z-index: 2;
      opacity: 0;
      animation: fadeInUp 1s ease-out forwards;
    }

    .page-hero h1 {
      animation-delay: 0.2s;
      font-size: 3rem;
      color: #001f4d;
      text-shadow: 0 0 10px #00bfff, 0 0 20px #00bfff, 0 0 40px #00bfff;
      letter-spacing: 2px;
      text-transform: uppercase;
      font-weight: 800;
      margin-bottom: 15px;
      animation: glowPulse 3s infinite alternate, fadeInUp 1s ease-out forwards;
    }

    .page-hero p {
      color: #fff;
      font-size: 1.2rem;
      text-shadow: 0 2px 6px rgba(0,0,0,0.8);
      animation-delay: 0.5s;
      max-width: 700px;
      line-height: 1.6;
    }

    @keyframes glowPulse {
      from { text-shadow: 0 0 10px #00bfff, 0 0 20px #00bfff, 0 0 40px #00bfff; }
      to { text-shadow: 0 0 20px #00ffff, 0 0 40px #00ffff, 0 0 80px #00ffff; }
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(40px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* ---------- SERVICES SECTION (ONLY BACKGROUND CHANGED) ---------- */
    .services-section {
      background-color: #ff9a00; /* CHANGED -> your color */
      color: #fff;
      padding: 80px 20px;
      text-align: center;
    }

    .services-section h2 {
      font-family: 'Merriweather', serif;
      font-size: 2rem;
      color: #fff;
      text-shadow: 0 0 10px rgba(0, 191, 255, 0.6);
      margin-bottom: 50px;
      font-weight: 700;
      animation: fadeInUp 1s ease-out;
    }

    .service-card {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background-color: #ffffff;
      border: 2px solid #00bfa6;
      border-radius: 15px;
      padding: 25px 35px;
      margin: 20px auto;
      max-width: 1000px;
      transition: 0.3s ease;
      box-shadow: 0 0 15px rgba(0, 255, 200, 0.15);
      opacity: 0;
      transform: translateY(60px) scale(0.95);
      will-change: transform, opacity;
    }

    .service-card.visible {
      opacity: 1;
      transform: translateY(0) scale(1);
      box-shadow: 0 0 25px rgba(0, 255, 200, 0.4);
    }

    .service-card:nth-child(even) {
      flex-direction: row-reverse;
    }

    .service-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 0 30px rgba(0, 255, 200, 0.4);
    }

    .service-icon {
      font-size: 2.8rem;
      color: #00bfa6;
      flex-shrink: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      width: 80px;
      height: 80px;
      background: rgba(0, 191, 166, 0.1);
      border-radius: 50%;
    }

    .service-content {
      flex: 1;
      margin: 0 25px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
    }

    .service-content h3 {
      color: #003c2b;
      font-size: 1.4rem;
      margin-bottom: 10px;
      font-family: 'Merriweather', serif;
    }

    .service-content p {
      color: #333;
      font-size: 0.95rem;
      line-height: 1.5;
      max-width: 600px;
    }

    /* 🔹 Animations */
    @keyframes slideInLeft { from {opacity:0; transform:translateX(-60px);} to {opacity:1; transform:translateX(0);} }
    @keyframes slideInRight { from {opacity:0; transform:translateX(60px);} to {opacity:1; transform:translateX(0);} }
    @keyframes slideInBottom { from {opacity:0; transform:translateY(60px);} to {opacity:1; transform:translateY(0);} }

    .slide-left.visible { animation: slideInLeft 0.7s ease-out forwards; }
    .slide-bottom.visible { animation: slideInBottom 0.7s ease-out forwards; }
    .slide-right.visible { animation: slideInRight 0.7s ease-out forwards; }

    /* ---------- RESPONSIVE ---------- */
    @media (max-width: 768px) {
      .page-hero { height: 65vh; }
      .page-hero h1 { font-size: 2rem; margin: 0 10px; }
      .page-hero p { font-size: 1rem; margin: 0 15px; }

      .service-card {
        flex-direction: column !important;
        text-align: center;
        padding: 30px 20px;
        width: 95%;
      }

      .service-content {
        margin: 0 !important;
        text-align: center !important;
        align-items: center !important;
      }
    }

    @media (max-width: 400px) {
      .page-hero h1 { font-size: 1.6rem; }
      .page-hero p { font-size: 0.9rem; }
      .service-icon { width: 65px; height: 65px; font-size: 2.2rem; }
      .service-content p { font-size: 0.9rem; }
    }
  </style>
</head>

<body>
  <%- include("partials/navbar"); %>

  <!-- HERO SECTION -->
  <section class="page-hero">
    <h1>SERVICES</h1>
    <p>Professional aerial services and DGCA-approved drone operations.</p>
  </section>

  <!-- SERVICES SECTION -->
  <section class="services-section">
    <h2>Explore Our Drone Services</h2>

    <div class="service-card slide-left">
      <div class="service-icon"><i class="fa-solid fa-map-location-dot"></i></div>
      <div class="service-content">
        <h3>Aerial Mapping</h3>
        <p>Precision mapping for agriculture, construction, and urban planning using advanced UAVs.</p>
      </div>
    </div>

    <div class="service-card slide-bottom">
      <div class="service-icon"><i class="fa-solid fa-wind"></i></div>
      <div class="service-content">
        <h3>Windmill Inspection</h3>
        <p>Detailed drone inspections for wind turbines, ensuring blade integrity, structural safety, and optimized performance.</p>
      </div>
    </div>

    <div class="service-card slide-right">
      <div class="service-icon"><i class="fa-solid fa-train"></i></div>
      <div class="service-content">
        <h3>Railway & Foot Over Bridge Inspection</h3>
        <p>High-precision drone surveys to assess structural stability, detect defects, and enhance maintenance safety for railway and foot over bridges.</p>
      </div>
    </div>

    <div class="service-card slide-left">
      <div class="service-icon"><i class="fa-solid fa-water"></i></div>
      <div class="service-content">
        <h3>Pipeline Inspection</h3>
        <p>Comprehensive aerial inspections to detect leaks, monitor corrosion, and ensure the safety and efficiency of long-distance pipeline networks.</p>
      </div>
    </div>

    <div class="service-card slide-bottom">
      <div class="service-icon"><i class="fa-solid fa-golf-ball-tee"></i></div>
      <div class="service-content">
        <h3>Golf Ground Inspection</h3>
        <p>High-resolution aerial surveys to assess turf health, irrigation systems, and course layout for maintenance and performance optimization.</p>
      </div>
    </div>
  </section>

  <%- include("partials/footer"); %>

  <script>
    // Animate cards when visible (down + up)
    const observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        } else {
          entry.target.classList.remove('visible'); // 👈 allows re-animation on scroll up
        }
      });
    }, { threshold: 0.2 });

    document.querySelectorAll('.service-card').forEach(card => observer.observe(card));
  </script>
</body>
</html>
