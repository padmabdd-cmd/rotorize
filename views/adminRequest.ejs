<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Access Requests | Admin</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: #0d1117;
      color: #fff;
      padding: 20px;
      margin: 0;
    }

    h1 {
      color: #00ffff;
      text-align: center;
      margin-bottom: 20px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: #161b22;
      border-radius: 8px;
      overflow: hidden;
    }

    th, td {
      padding: 12px;
      text-align: left;
      border-bottom: 1px solid #333;
    }

    th {
      background: #00bcd4;
      color: #fff;
      text-transform: uppercase;
      font-size: 0.95rem;
      letter-spacing: 0.5px;
    }

    tr:nth-child(even) {
      background: #1c1f26;
    }

    tr:hover {
      background: #21262d;
      transition: background 0.2s ease-in-out;
    }

    .date {
      color: #bbb;
      font-size: 0.9rem;
    }

    @media (max-width: 768px) {
      table, thead, tbody, th, td, tr {
        display: block;
      }
      thead { display: none; }

      tr {
        margin-bottom: 10px;
        border: 1px solid #333;
        border-radius: 8px;
        padding: 10px;
        background: #161b22;
      }

      td {
        display: flex;
        justify-content: space-between;
        border: none;
        padding: 8px 0;
      }

      td::before {
        content: attr(data-label);
        font-weight: 600;
        color: #00ffff;
      }
    }
  </style>
</head>
<body>
  <h1>🛡️ Access Requests</h1>

  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Reason</th>
        <th>Date</th>
      </tr>
    </thead>

    <tbody>
      <% let list = requests || []; %>

      <% if (list.length === 0) { %>
        <tr>
          <td colspan="4" style="text-align:center;">No access requests yet</td>
        </tr>
      <% } else { %>
        <% list.forEach(r => { %>
          <tr>
            <td data-label="Name"><%= r.name %></td>
            <td data-label="Email"><%= r.email %></td>
            <td data-label="Reason"><%= r.reason %></td>
            <td data-label="Date" class="date"><%= new Date(r.date).toLocaleString() %></td>
          </tr>
        <% }) %>
      <% } %>
    </tbody>
  </table>
</body>
</html>
